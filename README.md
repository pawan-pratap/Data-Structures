# Implementation of standard Data Structures(LANG: C). </br>
---

Note: The files don't include a main function. Therefore, it can be used by just importing the file.</br>
Note: In a circular linked list and a doubly Circular linked list, the head points to the last node. Doing so decreases the time complexity of adding a node at the beginning and at the end from O(n) to O(1).</br>
Note: In stack_ll.c, the printStack function prints the Stack from top to bottom as a result of its implementation design. </br>

---
